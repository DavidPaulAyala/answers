import { answerFrequency } from 'answers/helpers/answer-frequency';
import { module, test } from 'qunit';

module('Unit | Helper | answer frequency');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = answerFrequency([42]);
  assert.ok(result);
});
